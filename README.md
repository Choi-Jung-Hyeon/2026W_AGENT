# 🏫 SKKU Notice Agent (성균관대 공지사항 맞춤형 비서)

성균관대학교 학부 공지사항을 실시간으로 검색하고, 사용자의 개인 프로필(학과, 학년, 관심 분야 등)을 분석하여 **가장 적합한 공지를 추천 및 요약**해주는 AI 에이전트 챗봇입니다.

## 🌟 주요 기능

* **실시간 크롤링**: `requests`와 `BeautifulSoup`을 활용해 성균관대 공식 홈페이지의 최신 공지사항을 실시간으로 가져옵니다.
* **맞춤형 분석**: 사용자가 사이드바에 입력한 프로필(성적, 전공 등)을 바탕으로, AI가 공지의 자격 요건을 검토하여 지원 가능 여부를 판단합니다.
* **대화형 인터페이스**: Streamlit 기반의 채팅 UI를 통해 선배와 대화하듯 편하게 공지사항을 물어볼 수 있습니다.
* **지능형 검색**: 단순 키워드 매칭을 넘어, LLM(GPT-4.1-mini)이 공지의 맥락을 이해하고 관련성 높은 정보를 우선적으로 제안합니다.

## 🛠 기술 스택

* **Language**: Python 3.12+
* **Framework**: LangChain, LangGraph, Streamlit
* **LLM**: OpenAI GPT-4.1-mini
* **Library**: BeautifulSoup4, Requests, Python-dotenv

## 🚀 시작하기

### 1. 필수 라이브러리 설치

```bash
pip install streamlit requests beautifulsoup4 langchain-openai langgraph python-dotenv

```

### 2. API 키 설정

프로젝트 루트 디렉토리에 `key.env` 파일을 생성하고 OpenAI API 키를 입력합니다.

```env
OPENAI_API_KEY=your_openai_api_key_here

```

### 3. 실행 방법

Streamlit을 통해 웹 인터페이스로 실행합니다.

```bash
streamlit run school_bot.py

```

## 📂 프로젝트 구조

* `school_bot.py`: 메인 실행 파일 (UI 및 에이전트 로직)
* `key.env`: API 키 관리 파일 (보안 주의)
* `venv/`: 파이썬 가상 환경

## 💡 사용 팁

* **나의 프로필 작성**: 사이드바에 전공, 학점, 소득분위 등을 상세히 적을수록 장학금 추천의 정확도가 올라갑니다.
* **키워드 검색**: "장학금 신청하는 법"과 같은 긴 문장보다는 **"장학"**, **"인턴"**, **"7품"** 등 핵심 키워드로 질문하는 것이 크롤링 결과가 더 정확합니다.
